@model IEnumerable<SystemCri.Domain.Entities.Depto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeptoNom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeptoDescrip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeptoNom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeptoDescrip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeptoCod">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeptoCod">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeptoCod">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
